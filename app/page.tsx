// M3 SCAFFOLD — placeholder root. Product surfaces arrive with M5+ (master
// plan Phase B); this page only proves the canon shell (wash, Geist, tokens).
export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-16 pb-24 pt-8">
      <div className="font-mono text-xs uppercase tracking-widest text-stone-500">
        v2 · scaffold
      </div>
      <h1 className="mt-2 text-7xl font-bold tracking-tighter leading-none">
        Atlas<span className="text-amber-500">.</span>
      </h1>
      <p className="mt-8 text-sm italic text-stone-500">
        Nothing here yet. That&apos;s the point — surfaces arrive module by
        module.
      </p>
    </main>
  );
}
